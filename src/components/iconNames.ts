/**
 * The icon-name union is baked into src/lib/brand/iconNames.ts (icons:build),
 * where `lib` can read it without importing a component. Re-exported here for
 * the components that always imported it from this path.
 */
export * from "@/lib/brand/iconNames";
