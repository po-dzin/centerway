{
  "name": "centerway-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm run --prefix platform dev",
    "lint": "npm run --prefix platform lint",
    "build": "npm run --prefix platform build",
    "smoke": "npm run --prefix platform smoke",
    "userflows": "npm run --prefix platform userflows",
    "ci:smoke": "npm run --prefix platform ci:smoke"
  }
}
