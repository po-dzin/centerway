{
  "name": "centerway-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run -w apps/platform dev",
    "lint": "npm run -w apps/platform lint",
    "build": "npm run -w apps/platform build"
  }
}
