# Full QA — чеклист выхода MVP в продажу («открыть кран»)

Дата: 2026-09-01 · ревизия 2026-09-05 (аудит по коду и базе; см. «Что изменилось» ниже)
Статус: исполняемый чеклист Фазы 0 (см. `docs/centerway-deconstruction-roadmap-2026-09-01.md`). Каждый пункт привязан к файлу/команде. Порядок блоков — порядок исполнения; блок не начинается, пока предыдущий не зелёный.
Гейт выхода: **первая продажа по полной цене холодному покупателю.**

Роли: 🔧 — код/деплой (G), 👤 — ручная проверка в браузере/телефоне (соавтор может), 💳 — реальная транзакция (G).

---

## Что изменилось с 01.09 (ревизия 05.09)

- **Цена читается из строки базы, не из `products.ts`.** С 02.09 источник для курсов — `lms_course_offers`, для пакетов и заявок — `product_offers` (`/admin/catalog`). Константы `CW_TEST_PRICE_1UAH` в `products.ts` остались, но для way21/reset-day/short/irem мертвы: их никто не читает. Инструкция «grep и вернуть суммы» из старого Блока 2 не работает — цена меняется в админке.
- **way21 заряжает 4100 ₴** с 01.09. QA-окно для него закрыто.
- **reset-day заряжает 390 ₴, зачёркнуто 795** (строка выставлена 02.09). Оставлено как есть решением G от 05.09: вводная цена. Возврат к 795 — одно поле в админке.
- **herbs закрыт**: цена `null`, чекаут гаснет на всех дверях (лендинг, `/products/herbs`), лид-форма вместо кнопки. Откроется, когда владелец поставит цену в админке — без деплоя.
- **Коды заказов стали `course:<slug>`** (`course:way21`, `course:reset-day`). Вебхук WayForPay резолвит их через `loadPayableOffer`. **Ни один заказ через этот путь ещё не дошёл до `paid`**: последняя оплата — 22.08, reset-day за 1 ₴, старым кодом `reset-day`. С 01.09 по 05.09 создано 65 заказов, оплачено 0; 51 из них с одного IP (наши прогоны), два внешних (way21 04.09 вечером, natural-body 03.09) ушли на WayForPay и не вернулись — поднять в кабинете WFP.
- Meta Pixel на way21/reset-day подключён 05.09 через `/shared/js/landing-pixel.js` (тот же loader, что у short/irem: staff opt-out по `?cw_staff=1`, один pixel id, отложенная загрузка SDK). Clarity был подключён раньше в `js/common.js`.
- noindex снят 05.09 с way21, reset-day, dosha, consult. `/way21` и `/reset-day` добавлены в sitemap рядом с `/programs/*`-двойниками.
- В футерах way21/reset-day появилась ссылка на публичную оферту (`public-offer.html` лежал, но не был залинкован); иконка Telegram ведёт в support-бот, не в личный аккаунт.
- Появился `npm run wfp:refund <order_ref>` — dry-run печатает запрос, `--confirm` отправляет REFUND в WayForPay; сумма и валюта берутся из строки заказа, вернуть можно только `paid`.

## Блок 0. Инвентарь: что именно выпускаем

Продаём чекаутом (по `docs/checkout-test-flow-2026-08-21.md`). Цена — из строки базы, колонка «сейчас» снята с прода 05.09:

| Продукт | Заряжается сейчас | Зачёркнуто | Источник цены | Fulfilment |
| --- | --- | --- | --- | --- |
| way21 | 4100 | — | `lms_course_offers` | course `/learn/way21` |
| reset-day | **390** | 795 | `lms_course_offers` | course `/learn/reset-day` |
| short | 795 | — | `lms_course_offers` | course `/learn/short` |
| irem | 3950 | — | `lms_course_offers` | course `/learn/irem-gymnastics` |
| natural-body | 2900 | 4100 | `lms_course_offers` | course |
| herbs | не продаётся (`null`) | — | `product_offers`, kind checkout | cabinet, когда будет цена |
| way21-support | 9000 (котировка) | — | `product_offers`, kind lead | лид-форма |
| consult | — | — | `product_offers`, kind lead | лид-форма |

- [x] 🔧 herbs: решено — не выпускаем, пока нет цены; все двери закрыты (73f757a5).
- [x] 🔧 reset-day: 390/795 оставлено как есть (решение G 05.09).
- [ ] 👤 Зафиксировать список выпускаемых узлов недели 1: рекомендация — **way21 + reset-day** (курсовое fulfilment, полный контроль), short/irem не трогаем (уже живут), herbs/consult — лид.

## Блок 1. Финальный QA-прогон (окно 1 ₴ закрыто — прогон идёт по живой цене)

Смысл 05.09: доказать цепочку **после** переезда цен в базу и кодов в `course:*`. Самый дешёвый честный прогон — reset-day за 390 ₴, потом refund через `npm run wfp:refund`. Это одновременно и контрольная покупка Блока 2.

Для reset-day обязательно, для way21 — по решению G (4100 ₴ с последующим refund) (👤 + 💳, с телефона, не с дев-машины, чистый браузер/инкогнито):
- [ ] лендинг → CTA → чекаут WayForPay открывается, сумма и описание корректны;
- [ ] оплата картой → возврат на `/pay/thanks` (www, без 308-редиректа посреди возврата — проверить URL в адресной строке);
- [ ] на thanks: правильная кнопка fulfilment (в кабинет/курс, не в чужой бот);
- [ ] доступ реально открыт: `/learn/<slug>` пускает под тем аккаунтом, который платил; под чужим — нет;
- [ ] заказ виден в админке (`/admin/orders`), статус paid, product_code верный;
- [ ] Telegram-уведомление о заказе пришло в support-тред;
- [ ] письмо/подтверждение WayForPay дошло покупателю;
- [ ] **отказ**: прервать оплату → возврат на `/pay/failed`, страница внятная, есть путь назад к оплате;
- [ ] дошли события в Meta (Events Manager, Test Events): InitiateCheckout и Purchase с верным `content_name` и value (см. Блок 3 — сначала подключить пиксель);
- [ ] cron-напоминания: у свежего энролмента way21 (daily) появляется расписание (`lms_unstarted_reminders` / reminder log) — проверить запись в базе, не ждать суток.

Лид-формы (👤, по одной заявке):
- [ ] way21-support, consult, (herbs если лид): заявка → строка в `leads` → сообщение «📝 Нова заявка» в тред. `CW_trackLead` сработал (Pixel Helper).

- [ ] 💳 **Возврат денег**: `npm run wfp:refund <order_ref>` (dry-run печатает запрос и ничего не шлёт), затем тот же вызов с `--confirm`. Убедиться, что вебхук перевёл заказ в `refunded` (`/admin/orders`) и доступ закрыт/оставлен по политике. Первый настоящий refund не должен быть исследованием.

## Блок 2. Снятие QA-окна (цены)

- [x] 🔧 Цены сняты с 1 ₴ в базе: way21 4100, reset-day 390/795 (см. «Что изменилось»). `products.ts` больше не источник — грепать его бессмысленно.
- [ ] 🔧 Убрать мёртвые `TEST_PRICE_UAH`/`CW_TEST_PRICE_1UAH` из `products.ts` после первой оплаты, чтобы следующий читатель не искал цену там (не блокирует кран).
- [ ] 💳 **Одна контрольная покупка самому себе** — reset-day за 390 ₴ по цепочке Блока 1. Это первая транзакция через `course:*` и первая честная.
- [ ] 🔧 Refund контрольной покупки через `npm run wfp:refund` (или оставить как первый заказ — решение G).

## Блок 3. Трекинг перед трафиком

- [x] 🔧 Meta Pixel подключён в head way21 и reset-day через `/shared/js/landing-pixel.js` (05.09). Clarity — в `js/common.js` обоих лендингов.
- [x] 🔧 CAPI-двойник в коде: сервер (`jobs/worker.ts`) и клиент (`PurchaseSignal`) шлют Purchase с одним `event_id = purchase_<order_ref>`. Подтвердить в Events Manager на контрольной покупке.
- [ ] 👤 Pixel Helper на обоих лендингах: PageView, ViewContent, InitiateCheckout по CTA; `content_name` совпадает с `pixelContentName` (Way21 Detox / Reset Day — не переименовывать, это история отчётности).
- [ ] 👤 Clarity пишет сессии обоих лендингов.
- [ ] 🔧 Базовая панель узла: sessions → CTA-клик → checkout/lead. Минимум: Clarity + `/admin/analytics` + Meta. Записать «нулевую» строку недели, чтобы было с чем сравнивать.

## Блок 4. Открытие индексации

- [x] 🔧 noindex снят с way21 и reset-day (05.09).
- [x] 🔧 Остальные: dosha — снят, consult — снят, herbs — оставлен noindex (не продаётся), short-b — оставлен.
- [x] 🔧 sitemap: `/way21`, `/reset-day` добавлены к `/programs/*`; `my.`-хост закрыт в `robots.ts`.
- [ ] 👤 После деплоя: `curl -s <url> | grep robots` по каждому изменённому лендингу; Search Console — отправить sitemap.

## Блок 5. Trust-минимум на витрине (продаёт не пиксель)

- [ ] 👤 way21: 3 именованных, датированных отзыва формата «симптом → изменение» (собрать у реальных выпускниц Шляха; согласие на имя обязательно).
- [ ] 👤 «Пробный день» протокола way21 как превью/PDF — уже рекомендация buyer-council в `platform-scale-plan.md` Фаза 0.
- [ ] 🔧 «Апгрейд-залік»: 795 за reset-day идёт в зачёт way21 — одна строка на обоих лендингах + механика вручную (промокод / персональный оффер, инфраструктура персональных офферов irem уже есть).
- [x] 🔧 Контакт поддержки и иконка Telegram на way21/reset-day ведут в `centerway_support_bot` (05.09).
- [x] 🔧 Оферта и privacy залинкованы из футеров way21/reset-day (05.09).
- [ ] 👤 Реквизиты в `public-offer.html` обоих лендингов актуальны (требование WayForPay).

## Блок 6. Операционная готовность (что делать, когда пойдут живые люди)

- [ ] 👤 Договорка с соавтором: кто отвечает в support-треде и в какой SLA (например, до 4 ч днём).
- [ ] 👤 Сценарий «оплата прошла, доступа нет»: админский grant (`npm run lms:grant` / admin access) — прогнать один раз руками, чтобы в бою занимало 2 минуты.
- [ ] 👤 Сценарий «хочу возврат»: политика (в public-offer) + `npm run wfp:refund` из Блока 1.
- [ ] 👤 Раз в день глазами `/admin/orders`: заказ `created` с внешнего IP, не дошедший до `paid` — это либо живой человек, либо падение на возврате; сверять с кабинетом WayForPay.
- [ ] 🔧 Смоки зелёные на проде: `npm run smoke:admin`, `smoke:admin:funnel`, `lms:snapshot:check` — как пост-деплойный ритуал релиза Блоков 2–4.
- [ ] 🔧 Алерт «заказ создан, но не оплачен > 1 ч» — НЕ строить; вместо этого раз в день глазами `/admin/orders` (правило анти-роадмепа: не строить то, что заменяется взглядом, пока заказов < 10/день).

## Блок 7. Первый трафик (выход из Фазы 0 → Фаза 1)

- [ ] 👤 2 креатива × 2 аудитории на dosha и reset-day, бюджет $5–10/день.
- [ ] 👤 Назначен еженедельный «женя-гейт» (пн, 30 мин): spend / сессии / CTA / checkout / продажи / ascension.
- [ ] **Гейт закрыт: первая продажа полной цены холодному человеку.** Дата: ____

---

## Из чего вырастут следующие тикеты (не блокируют кран, уже в роадмепе)
Единый Offer (`PRODUCTS`→`lms_course_offers`); слоты §6.3 одной миграцией; `state.reported` + день 22; i18n `uk` в админке; membership.

## Правило недели запуска
Ни одного коммита в билдер/дизайн, пока чеклист не закрыт. Все 50–70 ч недели — сюда.
