name: admin-smoke

on:
  pull_request:
  push:
    branches:
      - main
      - "codex/**"

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SMOKE_BASE_URL: ${{ secrets.SMOKE_BASE_URL }}
      SMOKE_UI_BASE_URL: ${{ secrets.SMOKE_UI_BASE_URL }}
      SMOKE_ADMIN_BEARER: ${{ secrets.SMOKE_ADMIN_BEARER }}
      SMOKE_USER_BEARER: ${{ secrets.SMOKE_USER_BEARER }}
    steps:
      - uses: actions/checkout@v4

      # Version comes from .nvmrc so CI and local dev cannot drift apart.
      # They already did: node 20 ships npm 10, local dev runs npm 11, and the
      # two disagree on how nested optional deps are hoisted — so a lock
      # regenerated locally was rejected here by npm ci. Node 24 is LTS and
      # ships npm 11, which puts both sides on the same resolver.
      - uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install
        run: npm ci

      - name: Lint
        run: npm run -s lint

      - name: Typecheck
        run: npm run -s typecheck

      # With coverage: the thresholds in vitest.config.ts are a ratchet, and a
      # change that uncovers code fails here rather than in a report nobody reads.
      - name: Unit tests
        run: npm run -s test:coverage

      - name: LMS contract
        run: npm run -s lms:validate

      - name: Build
        run: npm run -s build

      - name: Admin Governance
        run: npm run -s smoke:admin:governance

      - name: Admin i18n / tone
        run: npm run -s smoke:admin:i18n-tone

      - name: Admin a11y contract
        run: npm run -s smoke:admin:a11y-contract

      - name: Admin authz coverage
        run: npm run -s smoke:admin:authz-coverage

      # No secret needed: playwright.config.ts starts `next start` on the build
      # above, and the thanks pages are static landings served from disk. This
      # is the browser coverage the job always has, whatever the secrets say.
      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Thanks-page browser smoke (no secrets)
        run: npm run -s smoke:thanks:browser

      - name: Admin API Smoke Suite (optional)
        if: ${{ env.SMOKE_BASE_URL != '' }}
        run: npm run -s smoke:admin:ci

      - name: Platform Browser Smoke (optional)
        if: ${{ env.SMOKE_UI_BASE_URL != '' }}
        run: npm run -s smoke:platform:browser
